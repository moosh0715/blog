const input=document.querySelector("#post-search");const cards=[...document.querySelectorAll(".post-card")];const empty=document.querySelector("#empty-state");if(input){input.addEventListener("input",()=>{const q=input.value.trim().toLowerCase();let shown=0;cards.forEach(card=>{const visible=!q||card.dataset.search.includes(q);card.hidden=!visible;if(visible)shown++});empty.hidden=shown!==0})}